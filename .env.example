# Required for production
DATABASE_URL="postgresql://postgres:password@localhost:5432/ruwaq?schema=public"
AUTH_SECRET=""  # openssl rand -base64 32
AUTH_URL="http://localhost:3000"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Google OAuth (optional — sign-in only works when set)
AUTH_GOOGLE_ID=""
AUTH_GOOGLE_SECRET=""

# OpenAI (optional — uses Arabic/English mock data when empty)
OPENAI_API_KEY=""

# App display name
NEXT_PUBLIC_APP_NAME="Ruwaq"

# Future / not used in MVP yet:
# STORAGE_ACCESS_KEY=
# STORAGE_SECRET_KEY=
# STRIPE_SECRET_KEY=
# RESEND_API_KEY=
